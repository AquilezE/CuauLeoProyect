<UserControl x:Class="Cliente.UserControllers.UserLobby"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Cliente.UserControllers"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="500">
    <Grid>
        <Border Background="LightGray" CornerRadius="5" BorderBrush="Gray" BorderThickness=".5" Margin="5">
            <Border.Effect>
                <DropShadowEffect Color="Black" Direction="315" ShadowDepth="2" Opacity="0.5"/>
            </Border.Effect>
            <Grid Width="500" MaxWidth="500" MinHeight="50">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60*"/>
                    <ColumnDefinition Width="20*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="{Binding Username}" Margin="10 0 0 0" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" Foreground="ForestGreen"/>

                <Button x:Name="KickButton" Grid.Column="1" Height="30" Width="70" VerticalAlignment="Center" Margin="0 0 0 10" Content="Kick" Click="KickButton_Click"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
